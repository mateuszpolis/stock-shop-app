import { createSlice } from "@reduxjs/toolkit";

export const similiarProductsSlice = createSlice({
  name: "similiarProducts",
  initialState: {
    similiarProducts: [],
  },
  reducers: {},
});

export default similiarProductsSlice.reducer;